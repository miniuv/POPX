# POPX Docs

Documentation for POPX — a custom family of operators that extends TouchDesigner's POPs (Point Operators).

**[View Documentation →](https://your-site-url.com)**

---

## Local Development

```bash
# Open index.html in a browser, or use a local server:
python -m http.server 8000
# Then visit http://localhost:8000
```

---

**Built with pure HTML, CSS, and JavaScript** • No build process required
